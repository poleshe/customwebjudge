result = getattr(step_funcs, step['basestep_name'])(user_id, test_id, count, step['argument'], step['description'], step['basestep_id'], step['basestep_name'] )



EJEMPLO REGISTER ONETOONE:

    u = User.objects.get(username='poleshe')
    
    # profile = Users.objects.create(user=u, user_id=2,email='test@gmail.com', role='profe', name='pol', surname='pol')

    test = u.users.email

    print(test)

